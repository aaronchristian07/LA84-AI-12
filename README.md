# LA84-AI-12

### How to run app.py
1. **Upgrade pip and install the streamlit library**
```bash
python -m pip install --upgrade pip
pip install streamlit
```

2. **Run the streamlit appliation**
```bash
streamlit run app.py
```
